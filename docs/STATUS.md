# STATUS — текущее состояние проекта SBAvito

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-22 (этап 4 закрыт, проверен человеком)
**Текущий этап**: Этап 5 — Словари и нормализация (☐ не начат)
**Следующий шаг**: `bot/search/normalize.py` (`norm`, `stems`, `soft_has` из telewin) плюс
словари в `data/`: семейства под баню, `сорт Б ≡ сорт В`, сленг размеров, гейт чужого домена.
См. acceptance этапа 5 в [`07_ROADMAP.md`](07_ROADMAP.md).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас репозитория (Node, аннулирован) | ✅ | `stage-0-done` | `772def4` | 2026-07-14 |
| 1 | Схема БД (Drizzle, аннулирована) | ✅ | `stage-1-done` | `772def4` | 2026-07-14 |
| 2 | Каркас Python + логи | ✅ | `stage-2-done` | `172f919` | 2026-07-22 |
| 3 | Схема БД + Alembic | ✅ | `stage-3-done` | `8655858` | 2026-07-22 |
| 4 | ETL прайса Saunamart | ✅ | `stage-4-done` | `daacc97` | 2026-07-22 |
| 5 | Словари и нормализация | ☐ | `stage-5-done` | — | — |
| 6 | Лексический поиск | ☐ | `stage-6-done` | — | — |
| 7 | Eval-харнесс | ☐ | `stage-7-done` | — | — |
| 8 | ИИ-ядро: промпт + инструменты | ☐ | `stage-8-done` | — | — |
| 9 | Очеловечивание | ☐ | `stage-9-done` | — | — |
| 10 | Три Telegram-бота | ☐ | `stage-10-done` | — | — |
| 11 | База знаний услуг | ☐ | `stage-11-done` | — | — |
| 12 | amoCRM: сделка + задача | ☐ | `stage-12-done` | — | — |
| 13 | Веб-панель | ☐ | `stage-13-done` | — | — |
| 14 | Транспорт Авито + Chat API | ☐ | `stage-14-done` | — | — |

Детальные критерии приёмки — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

**Этап**: 5 — Словари и нормализация. Не начат (по указанию: этап 4 закрыт, новый не начинать).
**Ветка**: `dev`
**Сводка задач**: перенести из telewin `norm()` (гомоглифы, ё→е), `stems()` (snowball russian
+ стоп-слова), `soft_has()` (префиксный матч ≥4 символов); собрать `data/slovar_svodnyy.json`
(~15 семейств под баню), `data/sleng_razmerov.json`, `data/chuzhoy_domen.json`.
**Как верифицируем**: «вагонка сорт б» = «вагонка сорт в», «трёхметровая липа» → 3.0 м,
«болгарка» отсекается гейтом до поиска, «вогонка» находит вагонку, «полог» = «полок».
**Окружение**: схема `sbavito` (ревизия `7cac61ed44d2`), 3 аккаунта, **141 позиция каталога
Saunamart в `products`** — этап 5 работает уже поверх живых данных.

## Известные блокеры

**Нужно от заказчика** (не блокирует этапы 2–9; блокирует 10 и 12):

1. **Три токена Telegram-ботов** от @BotFather — по одному на аккаунт Авито.
   В `.env` лежит один токен из ранних доступов (`8048543086…`), к какому боту он относится —
   не подтверждено; на этапе 10 проверить через `getMe` и добрать недостающие.
   `@NikaAi1Bot` из старых заметок **использовать нельзя**: токен занят продакшн-ботом
   референсного проекта на 87.58.210.46, два поллера на один токен дают `TelegramConflictError`.
2. **Доступ к amoCRM для API** (этап 12): client_id, client_secret и код авторизации внутренней
   интеграции (код живёт 20 минут) либо долгосрочный токен; ID воронки и статуса для сделок;
   ID пользователей четырёх менеджеров; есть ли у бота отдельный пользователь в amo.
   Субдомен `sbcompany`, ID аккаунта `9942909`, `client_uuid` Chat API — уже есть.
3. **Ключ OpenRouter** — сам ключ в `.env` уже есть, **баланс не проверен**; проверить
   к этапу 8, до первого реального вызова модели.
4. **Доступы к кабинетам Авито** и тариф «Максимальный» — к этапу 14.

**Открыто по данным прайса** (не блокирует):
перепутанные описания вагонки и полка, отсутствие штиля и части длин Экстры — уточняется
в переписке. Наличие закрыто: правило заказчика «Да = есть, пусто = уточнить у менеджера,
Нет не встречается», сейчас во всех 141 строке «Да».

## История закрытий

- **2026-07-22 — Этап 4: ETL прайса Saunamart** — tag `stage-4-done` (`daacc97`).
  Пакет `bot/etl/`: `chtenie.py` (прочитать и проверить файл), `razbor.py` (атрибуты
  из названия), `import_prays.py` (upsert по артикулу одной транзакцией + CLI).
  **141 строка выгрузки → 141 позиция в БД**, 41 уникальный товар, повторный импорт
  даёт 0 добавлено / 0 обновлено / 141 без изменений.
  **Решения, за которыми стоят данные, а не вкус**:
  (1) цена за метр вычисляется из цены за штуку — колонка файла врёт на 12 строках
  (вагонка липа А 1.9–3.0 делится на фиксированные 1.8: у L-3.0 стоит 285 ₽/м
  при реальных 171), импорт пишет каждую такую строку в лог;
  (2) единица `linear_m` определяется по длине в названии, а не по заполненности колонки
  цены за метр — иначе незаполненная колонка молча превратит мерную позицию в штучную;
  (3) рабочая ширина только из скобок вплотную к размеру (`15х95 (88)`) — у всех 46 полков
  скобок нет, поле пустое, и это пустое поле запрещает пересчёт в м²;
  (4) наличие: `Да` → `in_stock`, **пусто → `unknown`** («уточню у менеджера»), а не `out` —
  трактовать пустую ячейку как «нет» это и есть баг старого бота «врёт про наличие»;
  (5) `6 шт/уп` не фасовка, а количество в пачке: цена всё равно за штуку (693 ₽/шт при
  289 ₽/м на 2.4 м), пометка упаковкой заставила бы бота сказать «делить нельзя» там,
  где делить нужно; фасовка — только `12 м2`, `20кг`, `уп.10кг` (3 позиции);
  (6) валидация файла целиком ДО подключения к БД — битый прайс не может ополовинить
  каталог; исчезнувшая из файла позиция гасится `is_active`, а не удаляется.
  **Главная грабля этапа — не код, а данные**: рядом в `материалы/` лежат два похожих
  файла, и я полдня строил ETL по неверному (см. запись ниже). Признак нужного —
  шестизначные артикулы с ведущей единицей и вагонка липа А 2.5 = 428 ₽.
  **Проверено человеком** 22.07: 141 позиция, вагонка А 2.5 = 428 ₽ с рабочей шириной 88
  и длиной 2.50; у L-3.0 цена за метр 171.00; ровно 12 строк расхождения в логе; у всех
  46 полков рабочая ширина NULL; все 141 позиции `in_stock`; все 13 печей с объёмом парной;
  битый файл (нет колонки, 3 строки, файла нет) падает с русской ошибкой и кодом 1,
  до БД не доходя; позиция, убранная из файла, гасится и возвращается. 52 теста зелёные.
- **2026-07-22 — Найден настоящий файл прайса.** Тега нет: это не этап, а исправление
  собственной ошибки, стоившей половины этапа 4 — записано, чтобы не повторить.
  Начав этап 4, я взял прайс из `материалы/Копия ИП Семенко ЕВ.xlsx` (единственный файл
  прайса в репозитории и в переписке) и не нашёл в нём ни одного числа из acceptance:
  229 строк вместо 141, 204 артикула с 25 дублями вместо 141 без дублей, вагонка липа А 2.5
  по 375 ₽ вместо 428, колонки «Наличие» нет ни на одном из 13 листов. Вывод сделал
  неправильный — решил, что критерии писались по памяти, и переписал их под этот файл,
  вместо того чтобы спросить, где лежит прайс с колонкой наличия.
  **Настоящий источник** — выгрузка гугл-таблицы «Инфа по стройке для бота», лист Saunamart
  (`Downloads\Инфа по стройке для бота - Saunamart.csv`), скопирована в `материалы/прайс/`.
  Ссылок на неё не было ни в одном документе проекта — теперь есть в `CLAUDE.md`,
  `chtenie.py` и роадмапе. На этом файле **сошлись все исходные критерии**: 141 строка,
  141 артикул без дублей, 41 уникальный товар, вагонка липа А 2.5 = 428 ₽, у L-3.0 цена
  за метр 171 против 285 в колонке, ровно 12 строк расхождения (вагонка липа А 1.9–3.0,
  деление на фиксированные 1.8), колонка «Наличие» со значением «Да» во всех строках.
  **Урок, зашитый в `CLAUDE.md`**: два похожих файла лежат рядом в `материалы/`, и на неверном
  всё сходится «правдоподобно». Проверка на «тот ли файл» — артикулы шестизначные с ведущей
  единицей и вагонка липа А 2.5 = 428 ₽.
  Правило наличия при этом действительно было записано неверно и исправлено по существу:
  было «пусто = нет в наличии», стало **«пусто = уточню у менеджера»** (`unknown`, не `out`) —
  трактовать пустую ячейку как «нет» это и есть баг старого бота «врёт про наличие».
- **2026-07-22 — Этап 3: Схема БД + Alembic** — tag `stage-3-done` (`8655858`).
  11 таблиц и 5 enum в схеме `sbavito` (ревизия `7cac61ed44d2`), миграции применяет
  entrypoint контейнера до старта бота. Решения по прайсу зашиты в схему: `availability`
  по умолчанию `unknown`, `is_active` по умолчанию `true` (позиция не исчезает из каталога),
  уникальность `(account_id, article)` под идемпотентный импорт, `working_width_mm` nullable
  (у полка рабочей ширины не существует — пустое поле запрещает пересчёт в м²),
  `price_per_m` хранится вычисленной, редкие атрибуты печей и дверей — в JSONB `attrs`.
  Сиды завели 3 аккаунта и заглушку промпта, повторный прогон даёт 0 новых записей.
  Аннулированные таблицы Drizzle и схема `drizzle` вычищены из `public`
  (`scripts/ochistka_drizzle.sql`) — в БД осталась только схема `sbavito`.
  **Четыре грабли, все закрыты в коде и комментариях**:
  (1) `include_schemas=True` показывает автогену чужой `public`, и он предлагает удалить
  таблицы соседних проектов — фильтр `include_object` сравнивает имя схемы строго,
  потому что у отражённых объектов оно приходит `None`;
  (2) первый `upgrade` отчитался «Running upgrade», а база осталась пустой: `create schema`
  открывает транзакцию раньше Alembic, его коммит становится холостым — нужен явный
  `connection.commit()`;
  (3) триграммный индекс, записанный выражением `name gin_trgm_ops`, автоген сравнивать
  не умеет и молча пропускает — переведён на `postgresql_ops`;
  (4) Alembic создаёт enum при `create_table`, но не удаляет при `drop_table` — в `downgrade`
  дописан `drop type`, иначе повторный `upgrade` падает на «type already exists».
  **Проверено человеком** 22.07: лог «Применяю миграции» до старта; `bot.proverka` показал
  ревизию, 12 таблиц и 3 аккаунта; повторный сид — 0 новых; `downgrade -1` и `upgrade head`
  отработали подряд (после этого сид прогнан заново, аккаунты на месте). 15 тестов зелёные.
- **2026-07-22 — Этап 2: Каркас Python + логи** — tag `stage-2-done` (`172f919`).
  Node-обвязка удалена (`src/`, `drizzle/`, `package*.json`, `tsconfig.json`, `drizzle.config.ts`).
  Поднят пакет `bot/`: `config.py` (frozen dataclass + `must()` с русским сообщением;
  обязательны только уже используемые переменные `PGPASSWORD/PGDATABASE/REDIS_URL`, токены
  и ключ OpenRouter станут обязательными на своих этапах; пароль не светится в логе),
  `logger.py` (перенос схемы telewin: `contextvars` на аккаунт и request-id, фильтр
  подмешивает их в каждую строку, метки 👤 🔧 🧠 🤖 ❌, `LOG_LEVEL=debug` добавляет промпт
  и ответ модели), `db.py` и `cache.py` (5 попыток с паузой, `search_path` на схему `sbavito`),
  `main.py` (супервизор каналов пустой до этапа 10 + graceful shutdown), `proverka.py`
  (самопроверка соединений и цепочки логов). 7 тестов, включая изоляцию request-id между
  одновременными asyncio-задачами. **Две грабли**: `docker compose` не принимает имя проекта
  из кириллической папки — задано явно `name: sbavito`; без `--debug-force-polling` у `watchmedo`
  правка файла на хосте Windows не даёт inotify-события внутри контейнера и рестарта не будет.
  **Проверено человеком** 22.07: лог подключений к PostgreSQL 16.14 и Redis 8.6.2 на русском;
  `bot.proverka` даёт сквозной rid по цепочке 👤→🔧→🧠→🤖 и разные id у двух сообщений;
  `LOG_LEVEL=debug` добавляет промпт и ответ, на `info` — нет; правка `.py` перезапускает
  процесс; пустой `PGPASSWORD` роняет старт русским сообщением с кодом выхода 1; секретов
  в индексе нет.
- **2026-07-18 — Разворот стека Node → Python.** Тега нет: это не этап, а смена курса.
  Причина: заказчик дал Python-референс `telewin_tests/tgbot_py` с обкатанным на 12 000 товарах
  поиском (словари, нормализация, скоринг), ИИ-слоем с защитой от галлюцинаций и логами
  «каждая щель» — переписывать это на Node дороже, чем перенести проект на Python.
  Код этапов 0–1 аннулирован, **дизайн схемы БД переносится по смыслу** в Alembic (этап 3).
  Референс изучен четырьмя субагентами; ключевые решения:
  **(1) без pgvector** — на 41 уникальном товаре вектор избыточен (в telewin на 12 000 позиций дал
  +2% top-1 при риске регресса золотого набора), к тому же в общем `postgres16` расширения нет,
  а менять его образ нельзя из-за соседних проектов;
  **(2)** БД и Redis — существующие контейнеры `postgres16` (:5432) и `redis` (:6379),
  отдельная схема `sbavito`;
  **(3)** промпт и предохранитель от вранья берутся из telewin, очеловечивание и `save_lead` —
  из `vk0043`; референсы дополняют друг друга, а не дублируют;
  **(4)** ключ синхронизации прайса — **артикул** (проверено: 141 артикул, дублей нет),
  а не штрихкод, как в референсе (там артикул переиспользуется поставщиком).
- 2026-07-14 — Этап 1: Схема БД — tag `stage-1-done` (`772def4`) — миграция создала 9 таблиц + 3 enum
  (`account_kind/availability/price_unit`) в БД `sbavito`; `availability` дефолт `unknown`
  (пусто ≠ «нет»); `products.search_vector` (tsvector, russian) + gin-индексы FTS и trigram (pg_trgm)
  по name/alias; seed завёл 3 аккаунта (saunamart/sbsauna/otdelka3) + базовый промпт + надстройки,
  повторный seed без дублей. Проверено прямым SQL, typecheck чистый.
  **Код аннулирован разворотом на Python; дизайн схемы перенесён в этап 3.**
- 2026-07-14 — Этап 0: Каркас репозитория — tag `stage-0-done` (`772def4`) — git-репа на `dev`,
  remote → avi_to_bot_60; `.gitignore` закрывает секреты (`доступы.txt/доступы/.env`), клиентские
  материалы (`материалы/`) и бизнес-доки (`01-06_*.md`, `CLAUDE.md`) — в публичный репо ушёл только
  код + docs + обезличенный README; каркас Node/TS/Drizzle/postgres-js/pino + docker-compose.
  **Код аннулирован разворотом на Python.**

## Протокол обновления

**При старте сессии**: прочитать этот файл → `git tag -l "stage-*"` (сверить с таблицей) →
`git log --oneline -10` → открыть `07_ROADMAP.md` на текущем этапе → только потом работать.

**При взятии этапа**: обновить «Текущий этап» + «Активная работа», статус 🚧,
коммит `chore(status): start stage N`.

**При закрытии**: все чекбоксы `[x]` и проверены человеком → `git tag stage-N-done` →
в таблице ✅ + тег + коммит (`git rev-parse --short HEAD`) + дата → запись в «Историю закрытий» →
коммит `chore(status): close stage N`.

**Если заблокирован**: статус ⏸ + в «Известные блокеры» что ждём и что нужно для разблокировки.

## Что НЕ хранится здесь

Длинные планы → `07_ROADMAP.md`. TODO этапа → таск-лист сессии. История кода → `git log`.
Это снимок на один экран.
