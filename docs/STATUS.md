# STATUS — текущее состояние проекта SBAvito

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-16 (этап 2 — ETL прайса — взят в работу)
**Текущий этап**: Этап 2 — ETL прайса (🚧 в работе)
**Следующий шаг**: разобрать 13 листов `Копия ИП Семенко ЕВ.xlsx` в плоский прайс per аккаунт + отчёт по дублям/пустым остаткам. См. acceptance этапа 2 в [`07_ROADMAP.md`](07_ROADMAP.md).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас репозитория | ✅ | `stage-0-done` | `772def4` | 2026-07-14 |
| 1 | Схема БД | ✅ | `stage-1-done` | `772def4` | 2026-07-14 |
| 2 | ETL прайса | 🚧 | `stage-2-done` | — | — |
| 3 | Импорт прайса + Redis-кеш | ☐ | `stage-3-done` | — | — |
| 4 | Инструменты + сборка промпта | ☐ | `stage-4-done` | — | — |
| 5 | Eval-харнесс | ☐ | `stage-5-done` | — | — |
| 6 | Telegram-обкатка | ☐ | `stage-6-done` | — | — |
| 7 | Веб-панель | ☐ | `stage-7-done` | — | — |
| 8 | Транспорт Авито + amoCRM Chat API | ☐ | `stage-8-done` | — | — |

Детальные критерии приёмки — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

**Этап**: 2 — ETL прайса.
**Ветка**: `dev`
**Сводка задач**: разобрать 13 листов исходника, выбрать авторитетные, схлопнуть дубли, разложить цены из текста характеристик и нормализовать единицы, наличие из листа `остатки` (пусто → unknown), разнести позиции по аккаунтам.
**Как верифицируем**: плоский файл прайса на каждый аккаунт + отчёт (позиций / схлопнуто дублей / пустых остатков); спорные позиции из багов (необрезная, вагонка Экстра, осина Экстра) присутствуют с ценой и единицей.
**Важно**: исходник и выгрузки — клиентские данные, лежат в gitignored-путях, в публичный репо уходит только код ETL.

## Известные блокеры

Нет. (Канал amoCRM Chat API зарегистрирован 14.07.2026 — ключи в `доступы/`, `connect` на этапе 8. Доступы к кабинетам Авито + тариф «Максимальный» понадобятся к этапу 8.)

## История закрытий

- 2026-07-14 — Этап 1: Схема БД — tag `stage-1-done` (`772def4`) — миграция создала 9 таблиц + 3 enum (`account_kind/availability/price_unit`) в БД `sbavito`; `availability` дефолт `unknown` (пусто ≠ «нет»); `products.search_vector` (tsvector, russian) + gin-индексы FTS и trigram (pg_trgm) по name/alias; seed завёл 3 аккаунта (saunamart/sbsauna/otdelka3) + базовый промпт + промпт-надстройки, повторный seed без дублей. Проверено прямым SQL, typecheck чистый.
- 2026-07-14 — Этап 0: Каркас репозитория — tag `stage-0-done` (`772def4`) — git-репа на `dev`, remote → avi_to_bot_60; `.gitignore` закрывает секреты (`доступы.txt/доступы/.env`), клиентские материалы (`материалы/`) и бизнес-доки (`01-06_*.md`, `CLAUDE.md`) — в публичный репо ушёл только код + docs + обезличенный README; каркас Node/TS/Drizzle/postgres-js/pino + docker-compose; `db:migrate` подключается к `sbavito`.

## Протокол обновления

**При старте сессии**: прочитать этот файл → `git tag -l "stage-*"` (сверить с таблицей) → `git log --oneline -10` → открыть `07_ROADMAP.md` на текущем этапе → только потом работать.

**При взятии этапа**: обновить «Текущий этап» + «Активная работа», статус 🚧, коммит `chore(status): start stage N`.

**При закрытии**: все чекбоксы `[x]` и проверены → `git tag stage-N-done` → в таблице ✅ + тег + коммит (`git rev-parse --short HEAD`) + дата → запись в «Историю закрытий» → коммит `chore(status): close stage N`.

**Если заблокирован**: статус ⏸ + в «Известные блокеры» что ждём и что нужно для разблокировки.

## Что НЕ хранится здесь

Длинные планы → `07_ROADMAP.md`. TODO этапа → таск-лист сессии. История кода → `git log`. Это снимок на один экран.
